#include <stdio.h>
/**
 * add - add two integers
 * &a: first int
 * &b: second int
 * Return: addition
 */
int add(int a, int b)
{
	return (a + b);
}

/**                                                     * mod - calculates the remainder of two integers       * &a: first int                                        * &b: second int                                       * Return: remainder from division                                     */                                     int mod(int a, int b)                                   
{                                                              return (a % b);                                 
}

/**                                                     * sub - calculates the division of two integers       * &a: first int                                        * &b: second int                                       * Return: return th me calculate                                  
*/                         
int sub(int a, int b)                                     
{                                                              return (a - b);                                 }

/**                                                     * mul - calculates the remainder of two integers       * &a: first int                                        * &b: second int                                       * Return: addition                                     */                                                    int mul(int a, int b)                                      {                                                              return (a * b);                                 }

/**                                                     * div - calculates the remainder of two integers       * &a: first int                                        * &b: second int                                       * Return: division                                     */                                                    int div(int a, int b)                                      {                                                              return (a / b);                                 }
