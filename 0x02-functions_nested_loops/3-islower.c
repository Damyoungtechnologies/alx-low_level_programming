#include "main.h"
/**
* _islower - for lowercase check
* Return: returns either 1 or 0
* @c: parameter to be checked
*/
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')

		return (1);
	else
		return (0);


}
